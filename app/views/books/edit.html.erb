<div class="page-header">
  <h1>Editing: <%= @book.title %> <small><%= link_to "Delete", books_path(@book), method: :delete, "data-confirm"=>"Are you sure?" %></small></h1>
</div>
<%= render partial: "form" %>
